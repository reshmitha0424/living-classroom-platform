Legend: `[Active]`, `[Offline]`, `[Archive]`, `[Configuration]`

```text
project-root/
├── [Configuration] .env
├── [Configuration] .env.example
├── [Configuration] .gitignore
├── [Configuration] requirements.txt
├── [Configuration] files_explained.md
│
├── [Active] app.py
├── [Active] database_postgres.py
├── [Active] db_config.py
├── [Active] ingest.py
├── [Active] pull_historicData.py
│
├── [Configuration] .vscode/
│   ├── [Configuration] extensions.json
│   └── [Configuration] settings.json
│
├── [Active] templates/
│   ├── [Active] index.html
│   └── [Active] monitor_dashboard.html
│
├── [Active] static/
│   ├── [Active] monitor_script.js
│   ├── [Active] monitor_style.css
│   ├── [Active] script.js
│   ├── [Active] style.css
│   └── [Active] qr/
│       ├── [Archive] 1C43sl.jpg
│       └── [Active] 1C43sl.png
│
├── [Offline] scripts/
│   └── [Offline] offline/
│       ├── [Offline] best_birdwatching_hours.py
│       └── [Offline] train_activity_model.py
│
├── [Offline] data/
│   └── [Offline] reference/
│       └── [Offline] combined_birdweather_data.csv
│
├── [Archive] archive/
│   ├── [Archive] legacy_scripts/
│   │   ├── [Archive] birdweather_pull.py
│   │   ├── [Archive] database.py
│   │   └── [Archive] load_data.py
│   └── [Archive] sqlite_databases/
│       ├── [Archive] birds.db
│       └── [Archive] birds_v2.db
│
└── [Archive] __pycache__/
    ├── [Archive] db_config.cpython-313.pyc
    └── [Archive] pull_historicData.cpython-313.pyc
```

Active file purposes:

- `app.py` — Flask server, dashboard routes, PostgreSQL queries, JSON APIs, and activity forecasting.
- `database_postgres.py` — initializes the PostgreSQL `detections` table and unique index.
- `db_config.py` — loads PostgreSQL and BirdWeather configuration from `.env`.
- `ingest.py` — continuously retrieves current detections and inserts them into PostgreSQL.
- `pull_historicData.py` — retrieves historical detections over a configured date range.
- `templates/index.html` — interactive dashboard page.
- `templates/monitor_dashboard.html` — main monitoring display.
- `static/script.js` and `static/style.css` — behavior and styling for `index.html`.
- `static/monitor_script.js` and `static/monitor_style.css` — behavior and styling for the monitor dashboard.
- `static/qr/1C43sl.png` — QR image referenced by `monitor_dashboard.html`.

Files safe to remove now:

- `__pycache__/` — generated bytecode; Python recreates it.
- `static/qr/1C43sl.jpg` — no code or template references it.
- `files_explained.md` — optional and currently outdated; it mentions files that no longer exist in their old locations.

Safe to remove after confirming PostgreSQL data and backups:

- `archive/sqlite_databases/birds.db`
- `archive/sqlite_databases/birds_v2.db`
- `archive/legacy_scripts/`
- `data/reference/combined_birdweather_data.csv`

Potentially removable if offline analysis is no longer needed:

- `scripts/offline/best_birdwatching_hours.py`
- `scripts/offline/train_activity_model.py`

Those offline scripts still contain SQLite code and will not use the current PostgreSQL workflow without migration. No files were modified.

<hr>

## Active workflow files

These are part of the PostgreSQL application:

- `app.py` — Flask dashboard and API routes.
- `ingest.py` — live BirdWeather ingestion.
- `pull_historicData.py` — historical API ingestion.
- `database_postgres.py` — creates the PostgreSQL table and index.
- `db_config.py` — shared PostgreSQL connection settings.
- `templates/` — Flask HTML templates.
- `static/` — dashboard JavaScript and CSS.

## Good to keep, but outside the active workflow

- `combined_birdweather_data.csv` — useful as a backup/reference dataset, even if historical data will come from the API.
- `best_birdwatching_hours.py` — standalone CSV reporting utility; dashboard already provides similar information.
- `train_activity_model.py` — offline model evaluation and diagnostics; not used by the running dashboard.
- `.vscode/` — editor configuration; not required at runtime.

These two scripts still use SQLite. They must be migrated before running them against PostgreSQL.

## Obsolete or replaceable

- `database.py` — old initializer for `birds.db`.
- `database_v2.py` — obsolete duplicate of `database_postgres.py`; it also contains hard-coded credentials and should be removed or sanitized.
- `birdweather_pull.py` — early single-station API testing loop, superseded by `ingest.py`.
- `load_data.py` — old SQLite CSV importer. It is unnecessary if historical data will be pulled from the API.
- `birds.db` — original SQLite database.
- `birds_v2.db` — newer SQLite database, now replaced by PostgreSQL.
- `__pycache__/` — generated Python bytecode cache; safe to regenerate.
